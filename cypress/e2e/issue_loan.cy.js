describe('Issue Loan flow', () => {
  it('logs in and issues a loan (smoke)', () => {
    // This is a skeleton; requires a running server and seeded users
    cy.visit('/');
    // Implement login + navigate to Loans & EMIs and submit
  });
});

